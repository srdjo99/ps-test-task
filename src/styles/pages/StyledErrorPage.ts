import styled from 'styled-components';

const StyledNotAllowed = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
`;

const StyledErrorImg = styled.img`
  max-width: 500px;
  height: auto;
`;

export { StyledNotAllowed, StyledErrorImg };
